This directory contains simple test code for a user level process.
It simply loops and calls system call number 22 which puts a string onto
the console.

The files in this directory are:

Makefile          Compiles the source into a binary
test.c            Contains the simple test code
